// kono function lekhar somoy function likhe nise function ke call korte hoy
// Example 

function moneyBeg(){
    const money = 6000;
    console.log('I have ' + money + ' Taka')
}
moneyBeg()
// function er vitor er sokol kisu function er baire theke control kora jay na 
// Example 
// console.log(money)

function jogfol(input1, input2) {
    const sum = input1 + input2
    console.log('The sum is = ' + sum)
}
jogfol(60, 80);

function substraction(input01, input02) {
    const sub = input01 - input02
    console.log('The substraction is = ' + sub);
}
substraction(45898, 4584);

function multiplication(input001, input002) {
    let multi = input001 * input002;
    console.log('The Multiplication is ' + multi);
}
multiplication(45, 45)

function vagfol(input0001, input0002) {
    let vag = input0001 / input0002;
    console.log('The vagfol is ' + vag);
}
vagfol(45, 9)